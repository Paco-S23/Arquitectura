name: Keep Streamlit App Alive

on:
  schedule:
    - cron: "0 */2 * * *"   # Cada 2 horas
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: |
          curl -L https://arquitectura-behycqygjpbnqbd8wulurx.streamlit.app
